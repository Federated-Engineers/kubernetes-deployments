apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airbyte
  namespace: default
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    name: airbyte
spec:
  project: default

  source:
    repoURL: https://github.com/Federated-Engineers/kubernetes-deployments.git 
    targetRevision: HEAD
    path: applications/airbyte/
    helm:
      valueFiles:
      - values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
