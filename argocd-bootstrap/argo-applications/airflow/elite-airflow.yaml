apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: elite-airflow
  namespace: default
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    name: airflow
spec:
  project: default

  source:
    repoURL: https://github.com/Federated-Engineers/kubernetes-deployments.git 
    targetRevision: HEAD
    path: applications/charts/airflow/
    helm:
      valueFiles:
      - ../../production-values/elite-airflow-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: elite-airflow

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
